# Runtime outputs

Scripts write regenerated tables, models, logs and figures below this directory when configured for the review package. The directory is intentionally empty in version control because reference outputs and compact final source data are stored separately.

